#include<stdio.h>
#include<string.h>
int revstr(char str[])
{
	int i;
	int strleng=strlen(str);
	for(i=strleng-1;i>=0;i--)
	{
		printf("%c",str[i]);
	}
	
}
int main()
{
	char str[100];
	printf("enter the string:");
	scanf("%s",&str);
	printf("reverse string:");
	revstr(str);
}
